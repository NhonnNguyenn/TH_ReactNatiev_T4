# TypeScript OOP – 30 Câu

## Cách dùng nhanh
```bash
npm install
npm run build
node dist/index.js
```

- Mỗi bài nằm trong `src/q{1..30}.ts` (độc lập, có test console riêng).
- Commit từng câu:
```bash
git add src/q1.ts
git commit -m "Câu 1: Person"
git push
```
